# MAC 和 DCON 连接路线

核心区别：

- MAC：先扫描，扫到目标 MAC 后，用扫描结果 myDevice 去连接。
- DCON：不等扫描，直接拿 targetDeviceAddress 去连接。

## 公共入口

1. 串口收到 AT 命令。
2. serialEventTask() 从 Serial 读取数据，写入环形缓冲。
3. processDataTask() 从环形缓冲取出数据。
4. processATChar() 识别 AT+...\r\n。
5. processATCommand() 匹配具体命令。

## MAC 路线：扫描后连接

命令：

```text
AT+MAC=xx:xx:xx:xx:xx:xx
```

流程：

1. processATCommand() 匹配 MAC=。
2. isValidMacAddress() 校验 MAC。
3. targetDeviceAddress = 当前 MAC。
4. is_need_reset_adress = false。
5. ble_scan_over = false，表示允许后台扫描。
6. 如果当前已连接，并且 candeleteble 为 true，则 deinit_ble() 断开旧连接。
7. loop() 判断满足 !doConnect && !ble_connected && !ble_scan_over。
8. loop() 调用 start_ble_scan() 开始扫描。
9. MyAdvertisedDeviceCallbacks::onResult() 收到广播。
10. 如果 advertisedDevice.getAddress() == targetDeviceAddress，说明扫到目标设备。
11. BLEDevice::getScan()->stop() 停止扫描。
12. myDevice = new BLEAdvertisedDevice(...) 保存扫描结果。
13. doConnect = true。
14. loop() 调用 connectTobleServer()。
15. connectTobleServer() 发现 myDevice != nullptr，走扫描连接分支。
16. pClient->connect(myDevice)。
17. 连接成功后进入共同收尾流程。

## DCON 路线：跳过扫描直连

命令：

```text
AT+DCON=xx:xx:xx:xx:xx:xx
```

流程：

1. processATCommand() 匹配 DCON=。
2. isValidMacAddress() 校验 MAC。
3. targetDeviceAddress = 当前 MAC。
4. is_need_reset_adress = false。
5. ble_scan_over = true，避免 loop() 再触发 start_ble_scan()。
6. doConnect = true。
7. loop() 调用 connectTobleServer()。
8. connectTobleServer() 在 myDevice == nullptr 时走直连分支。
9. BLEAddress addr(targetDeviceAddress)。
10. pClient->connect(addr, BLE_ADDR_TYPE_RANDOM, 15000)。
11. 连接成功后进入共同收尾流程。

## 共同收尾流程

1. connectTobleServer() 调用 getService() 获取 fac/app/main/normal 服务。
2. 调用 getCharacteristic() 获取各通道特征。
3. ServerStateCheck() 注册 notify 回调。
4. ServerStateCheck() 返回 true。
5. connectTobleServer() 返回 true。
6. loop() 打印 AT+CONNECT_SUCCESS。

注意：connectTobleServer() 会优先判断 myDevice != nullptr；如果 DCON 前面残留了扫描得到的 myDevice，可能不会进入直连分支，而是走 pClient->connect(myDevice)。