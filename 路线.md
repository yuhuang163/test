# MAC 和 DCON 连接路线

核心区别：

- MAC：先扫描，扫到目标 MAC 后，用扫描结果 myDevice 去连接。
- DCON：不等扫描，直接拿 targetDeviceAddress 去连接。
- 当前主流程由 BLE 状态机驱动，不再靠 doConnect / ble_connected / ble_scan_over / candeleteble 这些全局 bool 组合判断。
- 状态和连接模式封装在 ble.cpp 内部，只通过 get_ble_state() / set_ble_state() / get_ble_connect_mode() / set_ble_connect_mode() 访问；每次状态变化都会打印 BLE_STATE: old -> new。

## 公共入口

1. 串口收到 AT 命令。
2. serialEventTask() 从 Serial 读取数据，写入环形缓冲。
3. processDataTask() 从环形缓冲取出数据。
4. processATChar() 识别 AT+...\r\n。
5. processATCommand() 匹配具体命令。

## MAC 路线：扫描后连接

命令：

```text
AT+MAC=xx:xx:xx:xx:xx:xx
```

流程：

1. processATCommand() 匹配 MAC=。
2. isValidMacAddress() 校验 MAC。
3. targetDeviceAddress = 当前 MAC。
4. is_need_reset_adress = false。
5. set_ble_connect_mode(CONNECT_BY_SCAN)。
6. 如果当前状态为 BLE_CONNECTED，则 deinit_ble() 断开旧连接，否则 clear_ble_scan_device() 清理旧扫描结果。
7. set_ble_state(BLE_SCANNING)。
8. loop() 在 BLE_SCANNING 状态下调用 start_ble_scan() 开始扫描。
9. MyAdvertisedDeviceCallbacks::onResult() 收到广播。
10. 如果 advertisedDevice.getAddress() == targetDeviceAddress，说明扫到目标设备。
11. BLEDevice::getScan()->stop() 停止扫描。
12. myDevice = new BLEAdvertisedDevice(...) 保存扫描结果。
13. set_ble_state(BLE_SCAN_FOUND)。
14. loop() 在 BLE_SCAN_FOUND 状态下切到 BLE_CONNECTING，并调用 connectTobleServer()。
15. connectTobleServer() 根据 get_ble_connect_mode() == CONNECT_BY_SCAN 走扫描连接分支。
16. pClient->connect(myDevice)。
17. 连接成功后进入共同收尾流程。

## DCON 路线：跳过扫描直连

命令：

```text
AT+DCON=xx:xx:xx:xx:xx:xx
```

流程：

1. processATCommand() 匹配 DCON=。
2. isValidMacAddress() 校验 MAC。
3. targetDeviceAddress = 当前 MAC。
4. is_need_reset_adress = false。
5. set_ble_connect_mode(CONNECT_DIRECT)。
6. clear_ble_scan_device() 清理旧扫描结果。
7. set_ble_state(BLE_CONNECTING)，避免 loop() 再触发 start_ble_scan()。
8. loop() 在 BLE_CONNECTING 状态下调用 connectTobleServer()。
9. connectTobleServer() 根据 get_ble_connect_mode() == CONNECT_DIRECT 走直连分支。
10. BLEAddress addr(targetDeviceAddress)。
11. pClient->connect(addr, BLE_ADDR_TYPE_RANDOM, 15000)。
12. 连接成功后进入共同收尾流程。

## 共同收尾流程

1. connectTobleServer() 调用 getService() 获取 fac/app/main/normal 服务。
2. 调用 getCharacteristic() 获取各通道特征。
3. ServerStateCheck() 注册 notify 回调。
4. ServerStateCheck() 返回 true。
5. connectTobleServer() 返回 true。
6. loop() set_ble_state(BLE_CONNECTED)。
7. loop() 打印 AT+CONNECT_SUCCESS。

注意：connectTobleServer() 现在优先看 get_ble_connect_mode()，不再用 myDevice != nullptr 猜连接路线；DCON 前会清理 myDevice，旧扫描结果不会影响直连分支。